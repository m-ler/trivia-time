'use client'

import { Box } from '@chakra-ui/react'

const SolidBackground = () => {
	return <Box position="fixed" inset={0} bgGradient="linear(to-tr, #576DEC, #2E92F2)" zIndex={-1} />
}

export default SolidBackground
